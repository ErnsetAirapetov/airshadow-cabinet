import type { ReactNode } from 'react';

/**
 * Оболочка простого режима: свой каркас навигации вместо апстримного `Layout`.
 *
 * ⚠️ Апстримный `src/components/layout/AppShell/**` НЕ трогаем — он остаётся
 * экспертному режиму. Здесь строится своя навигация: нижнее меню на четыре
 * пункта (главная, подписка, баланс, поддержка) и бургер (профиль, информация,
 * «Все возможности»).
 *
 * Пока это заглушка: каркас (#24) только заводит шов, навигацию наполняет
 * отдельная задача (#25). До неё простые страницы рендерятся без меню — это
 * ожидаемое промежуточное состояние, а не дефект.
 */
export function SimpleShell({ children }: { children: ReactNode }) {
  return <div className="min-h-screen">{children}</div>;
}
